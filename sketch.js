function setup() {
    // Função chamada ao iniciar a execução do código

    // Cria o canvas com a largura e altura especificadas
    createCanvas(1000, 600);
}

function draw() {
    // Função chamada a cada frame (quadro)

    // Limpa o plano de fundo e define a sua cor
    background(220);

    // Desenha os sprites na tela
    drawSprites();
}
